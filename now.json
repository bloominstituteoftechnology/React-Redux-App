{
  "version": 2,
  "name": "app",
  "builds": [
    {
      "src": "app/package.json",
      "use": "@now/static-build",
      "config": {
        "disDir": "build"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "app/$1" }]
}
