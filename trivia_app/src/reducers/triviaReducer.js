const initialState = {
    results: [],
    isLoading: true,
    error: "", 
}

export const triviaReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state; 
    }
}