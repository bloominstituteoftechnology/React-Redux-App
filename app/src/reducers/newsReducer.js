
export const initialState = {
    articles: [],
    isLoading: false,
    error: ''
} 

export const newsReducers = (state=initialState, action) => {
    switch(action.type) {
        
        default: 
        return state;
    }
}