
const initialState = {
    isLoading: false,
    pokemonData: [],
    error:""
}

export const pokemonReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}