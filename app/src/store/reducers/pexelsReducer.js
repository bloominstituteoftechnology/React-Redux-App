const initialState = {
    userEntry: "new york city",
    cityImage:"",
    isFetching: false,
    error:"",
}

export const pexelsReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}