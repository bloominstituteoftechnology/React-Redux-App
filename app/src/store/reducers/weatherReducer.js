const initialState = {
    isLoading: false,
    userEntry: "new york city",
    weatherData:"",
    isFetching: false,
    error:"",
}

export const weatherReducer = ( state =  initialState, action ) => {
    switch(action.type) {
        default :
        return state
    }
}