import React from "react";
import {
  Card,
  CardTitle,
  CardContent,
  CardHeader,
  Typography,
} from "@material-ui/core";

const Card = () => {
  return (
    <Card>
      <CardHeader />
    </Card>
  );
};

export default Card;
